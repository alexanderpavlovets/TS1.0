{
  "name": "ts-learning-project",
  "version": "0.0.1",
  "private": true,
  "description": "My own code on TS, maybe it will become a framework in the future",
  "scripts": {
    "postinstall": "webdriver-manager update",
    "pretest": "npm run tsc",
    "test": "protractor",
    "protractor": "protractor",
    "tsc": "tsc",
    "tscw": "tsc -w"
  },
  "dependencies": {
    "@types/node": "^7.0.18",
    "@types/jasmine": "2.5.35",
    "typescript": "^2.3.2",
    "protractor": "^5.1.1"
  }
}